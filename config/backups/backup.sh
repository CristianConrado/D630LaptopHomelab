I want to back up the data of docker images, that is why i saved them all under ~/docker/ so backing them up is easier.
I decided that one backup a week is more than enough, so I set up rclone to send that backup to my onedrive account, and also saved into a backups folder.
```bash

```
Should I save the stacks? Well I will because after all this is just for learning.
```bash

```
